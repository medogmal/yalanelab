// NavBar — removed from new design
// Each page/screen handles its own navigation
// This file kept for import compatibility only
export default function NavBar({ config }: { config?: any }) {
  return null;
}
